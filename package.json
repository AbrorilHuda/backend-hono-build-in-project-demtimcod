{
  "name": "learn-hono-bun",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "db:migrate": "bunx prisma migrate dev --name posts",
    "db:reset": "bunx prisma migrate reset"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0",
    "hono": "^4.6.14",
    "prisma": "^6.1.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
